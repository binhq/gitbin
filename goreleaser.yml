build:
    goos:
        - darwin
        - linux
    goarch:
        - amd64
    ldflags: "-s -w -X github.com/binhq/githubin/cmd.Version={{.Version}} -X github.com/binhq/githubin/cmd.CommitHash={{.Commit}} -X github.com/binhq/githubin/cmd.BuildDate={{.Date}}"

archive:
    replacements:
        dummy: dummy # Required because replacements are checked for length (https://github.com/goreleaser/goreleaser/blob/3ed26ed83b083dbd4267ad6bf5fc5282b2e5942c/pipeline/defaults/defaults.go#L53)
